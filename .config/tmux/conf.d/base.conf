# Configurações básicas

# ---------- [Configurações Essenciais] ----------
set -g default-terminal "xterm-256color"       # Mais compatível que tmux-256color
# set -as terminal-overrides ",xterm-256color:RGB"    # Alacritty precisa desta flag para cores verdadeiras
set -as terminal-overrides ',xterm-256color:RGB,:xterm-256color:delete=\E[3~'
# set -g focus-events on                         # Crucial para detectar aplicações focadas
# set -sg escape-time 10                         # Aumente para 10ms (0 pode causar problemas)

# ---------- [Controle do Mouse] ----------
set -g mouse on
set -g default-command 'zsh'

# ---------- [Teclas Básicas] ----------
# set -g base-index 1
# set -g renumber-windows on


# ---------- [Compatibilidade com Alacritty] ----------
# set -g terminal-overrides ',alacritty:RGB'     # Específico para Alacritty
